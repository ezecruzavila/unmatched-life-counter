# Dependencies & build output
node_modules
dist
dist-ssr
*.tsbuildinfo

# Local env / misc
*.local
.DS_Store

# Editor / tooling
.idea
.vscode
.cursor
/.claude

# Stray native leftovers (this is the web branch)
.gradle
/build
local.properties

# Local HTTPS certs (mkcert) — never commit private keys
.certs/
