<?xml version="1.0" encoding="utf-8"?>
<!-- Solo el contenedor redondeado; la flecha va en un ImageView aparte (siempre visible). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/setup_spinner_field_fill" />
    <corners android:radius="@dimen/setup_spinner_corner_radius" />
    <stroke
        android:width="@dimen/setup_ui_stroke_width"
        android:color="@color/setup_ui_border_gray" />
</shape>
